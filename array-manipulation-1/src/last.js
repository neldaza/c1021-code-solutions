/* exported last */
// 1. define last function with array as perameter
// 2. assign length of array minus 1 to variable lastChar
// 3. return the value of lastChar at array
function last(array) {
  var lastChar = array.length - 1;
  return array[lastChar];
}
