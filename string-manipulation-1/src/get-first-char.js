/* exported getFirstChar */
// 1. Create an action that is named properly for getting the ,
// last character, and allow an input to be given.
// 2. looks at current input, and grabs the first value,
// and assigns this to a new storage
// 3. returns the value of the new storage value in step 2

function getFirstChar(string) {
  var firstChar = string[0];
  return firstChar;
}
