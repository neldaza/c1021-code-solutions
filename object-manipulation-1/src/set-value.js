/* exported setValue */
// 1. define function set value to assign a new key and value to an object
// 2. the value is assigned to key at object
function setValue(object, key, value) {
  object[key] = value;
}
